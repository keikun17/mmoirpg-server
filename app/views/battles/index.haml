%h2 Battles

%p
  = link_to "Start battle", start_battles_path, class: 'btn btn-primary'

%p
  %table.table
    %tr
      %th ID
      %th Started
      %th Match count
      %th
    - @battles.each do |battle|
      %tr
        %td= battle.id
        %td= link_to battle.created_at, battle
        %td= battle.character_battles.count
        %td= link_to "delete", battle, method: :delete, confirm: 'Delete?'

